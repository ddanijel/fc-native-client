const initialState = {
    scannedProductTags: [],
    actions: [],
    activeProducer: {},
    jwtToken: null,
    signInData: {},
    signUpData: {}
};


const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;